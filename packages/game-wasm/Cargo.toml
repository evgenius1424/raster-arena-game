[package]
name = "client_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
physics_core = { path = "../../crates/physics_core" }
binary_protocol = { path = "../../crates/binary_protocol", features = ["wasm"] }

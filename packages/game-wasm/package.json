{
  "name": "@game/wasm",
  "version": "0.0.1",
  "private": true,
  "main": "pkg/wasm.js",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --out-name wasm",
    "format": "cargo fmt -p client_wasm"
  },
  "files": ["pkg"]
}

{
  "name": "@game/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd ../.. && cargo run -p raster-arena-server",
    "build": "cd ../.. && cargo build -p raster-arena-server --release",
    "preview": "cd ../.. && ./target/release/raster-arena-server",
    "format": "cargo fmt -p raster-arena-server"
  }
}
